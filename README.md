# Seismology part of LWSSPY 

---

## Installation

### Installation using Pip

However, 

```bash
git clone git@github.com:lsawade/lwsspy.seismo.git
cd lwsspy.seismo
pip install .
```

Should work. Use `pip install -e .` for development mode.
